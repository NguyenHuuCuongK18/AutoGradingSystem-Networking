<UserControl x:Class="TestKitGeneration.Views.GenerateScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestKitGeneration.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <TextBlock Text="Test Case Name:" Margin="5"/>
        <TextBox Text="{Binding TestCaseName, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5"/>
        <TextBox Text="{Binding ConsoleOutput}" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Visible" Height="300" Margin="5"/>
        <TextBlock Text="{Binding StageLabel}" Margin="5"/>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Text="Input:" Margin="5"/>
            <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5"/>
            <Button Content="Submit Input" Command="{Binding SubmitInputCommand}" Margin="5"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <TextBlock Text="Total Points:" Margin="5" VerticalAlignment="Center"/>
            <TextBox Text="{Binding TotalPoints, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="5"/>
            <Button Content="Record" Command="{Binding RecordCommand}" Margin="5"/>
            <Button Content="End Processes" Command="{Binding EndProcessesCommand}" Margin="5"/>
            <Button Content="Back to View" Command="{Binding BackToViewCommand}" Margin="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>